name: ui_error_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.4.0
  provider: ^6.1.5
  mocktail: ^1.0.4
  sqflite: ^2.4.2
  shared_preferences: ^2.5.3
  drift: ^2.26.1
  drift_flutter: ^0.2.4
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  drift_dev: ^2.26.1
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
