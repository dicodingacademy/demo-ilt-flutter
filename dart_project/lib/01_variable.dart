void variable() {
  var name = "Dico"; // can be reassigned
  final weight; // can be assigned later but only once
  const isMarried = true; // must define now and cannot be reassigned
  late int age; // declared now, set later
  weight = 50.5;
  age = 30;
}
