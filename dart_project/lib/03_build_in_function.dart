void buildInFunction() {
  var name = "Dico";
  int oneMillion = 1_000_000;
  num rating = 4.9;
  double distance = 10.67;

  print(name.toUpperCase()); // convert to upper case text
  print(oneMillion.isEven); // check whether is even or not
  print(rating.ceil()); // round the number up (int)
  print(distance + (10)); // use operator
}
