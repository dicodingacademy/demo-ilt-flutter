name: networking_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.4
  http: ^1.3.0
  mocktail_image_network: ^1.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
  assets:
    - assets/json/
